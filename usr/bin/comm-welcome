#!/usr/bin/env python3
"""Resolve the checkout or installed entry point."""

import runpy
import sys
from pathlib import Path

directory = Path(__file__).resolve().parents[1] / "share/comm-welcome"
sys.path.insert(0, str(directory))
runpy.run_path(str(directory / "main.py"), run_name="__main__")
